import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  radioButton: {
    display:'flex',
    flexDirection:'row',
    alignItems: 'center',
  },
  text: {
    fontSize: 18
  }
});

export default styles;
