import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  checkBox: {
    display:'flex',
    flexDirection:'row',
    alignItems: 'center',
    width: '90%',
    padding: 5,
    marginBottom: 10
  },
  text: {
    fontSize: 18,
    marginLeft: 5
  }
});

export default styles;
