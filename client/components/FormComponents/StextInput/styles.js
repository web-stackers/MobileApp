import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    height: 100,
    width: 380,
    margin: 7,
    borderWidth: 1,
    padding: 2,
    color: 'white',
  },
});

export default styles;
