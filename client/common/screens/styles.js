import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#652C9E',
    width: '100%',
    alignItems: 'center',
  },

  text: {fontSize: 40, color: 'white', marginTop: 300},
});

export default styles;
