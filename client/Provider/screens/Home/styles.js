import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 15,
  },
  logo: {
    marginTop: 100,
    alignSelf: 'center',
  },
});

export default styles;
